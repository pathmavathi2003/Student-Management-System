# Student Management System

A Java-based console application to manage student records using Object-Oriented Programming (OOP) principles.

## 🛠️ Concepts Used
* **Encapsulation:** Private fields in `Student` class with getters and setters.
* **Collections:** Managed dynamic records using Java Collections.
* **OOP Principles:** Modular code structure for clean logic.
